此作業有兩部分，分成part_one.py及part_two.py兩部分進行實作。

在跑part_one.py時，不需輸入任何字元。一開始會跑出輸入資料點的分佈，並標以顏色以區別組別。接著會跑出generative model的結果。然後會依序跑出discriminative model的結果，每5次迭代跑出一張圖，直至收斂。

在跑part_two.py時，亦不需輸入任何字元。一開始會會跑出輸入資料點的分佈，並標以顏色以區別組別。接著會跑出generative model的結果。然後會依序跑出discriminative model的結果，每5次迭代跑出一張圖，直至收斂。
